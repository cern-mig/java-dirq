Change log for java-dirq:

1.8	Wed Jun 29 2016
	* now using the findbugs maven plugin
	* now using SLF4J to log warnings (new runtime dependency)
	* the get() method can now throw an IOException
	* the getAsByteArray() method can now throw an IOException

1.7	Fri Nov 27 2015
	* now using the checkstyle maven plugin
	* now using Java NIO instead of POSIX calls through JNA
	* removed all external runtime dependencies (jna and commons-lang3)
	* the remove() method can now throw an IOException

1.6	Tue Nov  3 2015
	* major code cleanup

1.5	Tue Mar 31 2015
	* fixed NPE in QueueSimple's count() method

1.4	Mon Jul 15 2013
	* changed all the tests to use a temporary directory
	* cleaned the documentation
	* simplified the signature of the purge() methods
	* simplified the signature of the QueueSimple creator
	* added support for QueueSimple's rndHex attribute
	* added support for QueueSimple's maxLock and maxTemp attributes

1.3	Fri Apr 12 2013
	* first version recording changes...
